var assert = require('assert');
const {timeout} = require('../service/service')

describe('Requisição teste',()=>{
    describe('timeout',()=>{
        it('deve fazer uma requisição com timeout,e deve disparar o cliente de email',(done)=>{
            assert.equal(timeout)
        })
    })
})